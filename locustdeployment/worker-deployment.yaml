apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "1"
  labels:
    role: locust-worker
  name: locust-worker
spec:
  replicas: 2
  selector:
    matchLabels:
      role: locust-worker
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        role: locust-worker
    spec:
      containers:
      - image: gcr.io/gcpproject1/locustloadtest
        imagePullPolicy: IfNotPresent
        name: locust-worker
        env:
          - name: USE_HOST
            valueFrom:
              configMapKeyRef:
                name: locust-cm
                key: USE_HOST
          - name: LOCUST_MODE
            value: WORKER
          - name: LOCUST_MASTER_HOST
            value: locust-master
        volumeMounts:
          - mountPath: /locust
            name: locust-scripts
        resources: {}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
      volumes:
      - name: locust-scripts
        configMap:
          name: scripts-cm